server:
  port: ${PORT:8082}

spring:
  config:
    import: classpath:application-common.yml
  application:
    name: user-service
  datasource:
    url: jdbc:${USER_DB_URL:postgresql://localhost:5432/user_db}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: ${SHOW_SQL:true}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false  # Temporarily disable validation
    baseline-version: 0
    locations: classpath:db/migration
    table: flyway_schema_history
  servlet:
    multipart:
      max-file-size: ${MAX_FILE_SIZE:5MB}     # Increase from default 1MB
      max-request-size: ${MAX_FILE_SIZE:5MB}   # Increase from default 1MB
      enabled: true
      file-size-threshold: 2KB

logging:
  level:
    com.godayana: DEBUG
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

eureka:
  client:
    enabled: false  # Disable Eureka for local development
    register-with-eureka: false
    fetch-registry: false

FILE_SERVICE_URL: ${FILE_SERVICE_URL}
AUTH_SERVICE_URL: ${AUTH_SERVICE_URL}